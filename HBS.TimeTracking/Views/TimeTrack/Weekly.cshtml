@model List<HBS.Data.Entities.TimeTracking.Models.WeeklyTimeTrack>
@{
    ViewBag.Title = "Weekly";
}

<h2>Weekly</h2>

<table>
    <tr>
        <th>
            Week Number
        </th>
        <th>
            Week Start Date

        </th>
        <th>
            Week End Date
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.WeekNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WeekStartDate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.WeekEndDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
            @Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })
        </td>
    </tr>
}

</table>
